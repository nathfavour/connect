import { Client, TablesDB, Account } from 'node-appwrite';
import fs from 'fs';

// Try to load envs from an appwrite.config.json or something
// We can test locally with just a fake call if we get the API key
